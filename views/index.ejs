<%  include _header%>
<%if(!currentZombie){%>
  <h1> Bienvenid@ a la guarida zombie, registrate para seguir esta red social! </h1>
<%}%>

<%if(currentZombie){%>
  <h1> Hola <%= currentZombie.name()%>, este es tu ejercito Zombie!</h1>
  <%zombies.forEach((zombie)=>{ %>
  <div class="panel panel-default">
    <div class="panel-heading">
      <a href="/zombies/<%=zombie.username%>">
      <%=zombie.name() %>-Rango= <%=zombie.role%></a>  
    </div>
    <%if(zombie.bio){ %>
      <div class="panel-body"><%=zombie.bio %></div>
    <%}%>
</div>

<%})%>
<% } %>

<% include _footer%>